---
layout: none
sitemap: false
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">

{%- assign default_lang = site.default_lang | default: "en" -%}
{%- assign pages_and_posts = site.pages | concat: site.posts -%}

{%- for node in pages_and_posts -%}
  {%- assign node_lang = node.lang | default: default_lang -%}
  {%- if node_lang != default_lang -%}
    {%- assign canonical_path = node.url | remove_first: "/" | remove_first: node_lang -%}
  {%- else -%}
    {%- assign canonical_path = node.url -%}
  {%- endif -%}
  {%- assign node.canonical_path = canonical_path -%}
{%- endfor -%}

{%- assign url_groups = pages_and_posts | group_by: "canonical_path" -%}

{%- for group in url_groups -%}
  {%- assign canonical_node = group.items | where:"lang",default_lang | first -%}
  {%- if canonical_node -%}
  <url>
    <loc>{{ canonical_node.url | absolute_url }}</loc>
    <lastmod>{{ canonical_node.last_modified_at | default: canonical_node.date | default: site.time | date_to_xmlschema }}</lastmod>

    {%- for alternate_node in group.items -%}
      {%- assign alternate_lang = alternate_node.lang | default: default_lang -%}
      {%- if alternate_lang == default_lang -%}
        {%- assign alternate_url = alternate_node.url -%}
      {%- else -%}
        {%- assign alternate_url = "/" | append: alternate_lang | append: alternate_node.url -%}
      {%- endif -%}
      <xhtml:link
        rel="alternate"
        hreflang="{{ alternate_lang }}"
        href="{{ alternate_url | absolute_url }}"
      />
    {%- endfor -%}

    <xhtml:link
      rel="alternate"
      hreflang="x-default"
      href="{{ canonical_node.url | absolute_url }}"
    />
  </url>
  {%- endif -%}
{%- endfor -%}

</urlset>